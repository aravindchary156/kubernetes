apiVersion: batch/v1
kind: Job
metadata:
  name: my-job
spec:
  template:
    metadata:
      name: my-job-pod
    spec:
      containers:
        - name: job-container
          image: busybox
          command: ["echo", "Hello Kubernetes"]
      restartPolicy: OnFailure

